# Отчёт по итоговому проекту по курсу «Инженерия Искусственного Интеллекта»

> Рекомендуемый объём отчёта: 3-5 страниц в эквиваленте Markdown/печатного текста.  
> Отчёт должен позволить преподавателю понять задачу, данные, выбранные модели и результаты экспериментов.

---

## 1. Паспорт проекта

- **Название проекта:** `ML-сервис для автоматической классификации тональности пользовательских отзывов`
- **Создатели проекта:**
  - `Данишкин Антон Евгеньевич` — разработка основной части проекта: EDA, обучение моделей, FastAPI-сервис, Docker, тесты, документация.
  - `Черноскутов Артём Иванович` — разработка дополнительного функционала / усложнений проекта.
- **Группа:** `<Группа ИКБО-09-22>`
- **Контакты:** Участника 1 `<@Deadoxid или danishkin95@mail.ru, Данишкин Антон>`
- **Контакты:** Участника 2 `<Информация об Артёме>`
- **Ссылка на репозиторий:** Участника 1 `<https://github.com/Deadoxide/Danishkin_Anton>`
- **Ссылка на репозиторий:** Участника 2 `Ссылка`

Проект посвящён разработке ML-сервиса для автоматического анализа тональности пользовательских отзывов.  
Сервис принимает на вход текст отзыва и возвращает один из трёх классов: `negative`, `neutral` или `positive`, а также confidence score.  
В проекте реализован полный цикл: EDA, обучение baseline-моделей, выбор финальной модели, сохранение sklearn Pipeline, FastAPI-сервис, тесты и Docker-контейнеризация.

---

## 2. Постановка задачи и контекст

### Предметная область и задача

Предметная область проекта — анализ пользовательской обратной связи.  
Такая задача может быть полезна интернет-магазинам, службам поддержки и аналитическим отделам, которым нужно быстро понимать общее настроение клиентов по отзывам.

Проект решает задачу многоклассовой классификации текста.  
Пользователь вводит текст отзыва, а сервис автоматически определяет его тональность.

Возможный сценарий использования:

1. Пользователь или внешняя система отправляет текст отзыва в API.
2. Сервис обрабатывает текст.
3. Модель определяет тональность отзыва.
4. API возвращает класс тональности и уверенность модели.

### Формулировка задачи в терминах ML

- **Входные данные:** текст пользовательского отзыва.
- **Выход модели:** класс тональности:
  - `negative`;
  - `neutral`;
  - `positive`.
- **Тип задачи:** supervised text classification.
- **Модельный подход:** TF-IDF-векторизация текста и классические ML-модели.
- **Основные требования:** сервис должен быстро отвечать на запрос, использовать реальную обученную модель, возвращать понятный JSON-ответ и поддерживать запуск через API.

Пример входа:

```json
{
  "text": "Качество отличное, товар понравился"
}
```

Пример выхода:

```json
{
  "sentiment": "positive",
  "confidence": 0.971962339335859
}
```

### Целевые метрики качества

Для оценки моделей использовались:

- `accuracy`;
- `precision_macro`;
- `recall_macro`;
- `macro-F1`.

Основной метрикой выбрана `macro-F1`, так как задача является многоклассовой, и важно учитывать качество классификации по всем трём классам, а не только общую долю правильных ответов.

---


## 3. Данные

## Источник данных

В проекте используется открытый датасет RuReviews:

```text
https://github.com/sismetanin/rureviews
```

Файл датасета:

```text
women-clothing-accessories.3-class.balanced.csv
```

Датасет содержит русскоязычные отзывы о женской одежде и аксессуарах.

### Структура данных

Исходный датасет содержит:

- 90 000 строк;
- 2 колонки.

Колонки:

Колонка Описание
`review` текст пользовательского отзыва
`sentiment` метка тональности

Исходное распределение классов:

Класс Количество
negative 30000
neautral 30000
positive 30000

В исходных данных была обнаружена опечатка в названии класса `neautral`.  
На этапе предобработки эта метка была исправлена на `neutral`.

### Предобработка и EDA

Основной анализ данных выполнен в ноутбуке:

```text
notebooks/eda.ipynb
```

В ходе EDA были проверены:

- размер датасета;
- названия колонок;
- распределение классов;
- пропуски;
- дубликаты;
- длина отзывов в символах и словах.

Результаты проверки качества данных:

Проверка Значение
Пропуски в `review` 0
Пропуски в `sentiment` 0
Дубликаты по тексту отзыва 2679

Основные шаги предобработки:

- исправление метки `neautral` на `neutral`;
- приведение текста к нижнему регистру;
- замена `ё` на `е`;
- удаление ссылок;
- удаление лишних символов;
- нормализация пробелов;
- удаление пустых строк;
- удаление дубликатов по тексту отзыва.

После очистки подготовленный датасет содержит 86 146 строк.

Распределение классов после очистки:

Класс Количество
positive 28956
negative 28817
neutral 28373

Датасет остался достаточно сбалансированным, поэтому подходит для обучения моделей классификации.

Результаты EDA сохранены в:

```text
reports/eda_summary.md
reports/class_distribution.png
reports/review_length_distribution.png
```

---

## 4. Модели и подходы

Опишите, какие модели вы пробовали и как развивался ваш подход:

### Baseline-модели

Для решения задачи были выбраны классические модели машинного обучения для текстовой классификации.

Эксперименты выполнены в ноутбуке:

```text
notebooks/baseline_models.ipynb
```

Использовались следующие модели:

1. `DummyClassifier`;
2. `MultinomialNB`;
3. `LogisticRegression`.

Для преобразования текста в признаки использовался `TfidfVectorizer`.

`DummyClassifier` использовался как нижняя граница качества.  
`MultinomialNB` был выбран как быстрый классический алгоритм для текстовой классификации.  
`LogisticRegression` была выбрана как более сильный baseline для разреженных TF-IDF-признаков.

### Финальная модель

Финальной моделью выбрана связка:

```text
TfidfVectorizer + LogisticRegression
```

Она показала лучшее качество среди протестированных baseline-моделей по `macro-F1`.

Финальная модель обучается в:

```text
src/train.py
```

Сохранённая модель находится в:

```text
artifacts/sentiment_model.joblib
```

Метаданные модели сохраняются в:

```text
artifacts/model_metadata.json
```

Нейросетевые модели и трансформеры в текущей версии проекта не использовались, так как цель проекта — реализовать стабильный BASIC/BASIC+ end-to-end ML-сервис на классическом ML-подходе.

---

## 5. Экспериментальный протокол и результаты

### Экспериментальный протокол

Для экспериментов использовался подготовленный датасет после очистки.

Разделение данных:

- train: 80%;
- test: 20%.

Разделение выполнялось с параметрами:

- `random_state = 42`;
- `stratify = y`.

Стратификация использовалась для сохранения распределения классов в обучающей и тестовой выборках.

Модели оценивались на тестовой выборке по следующим метрикам:

- `accuracy`;
- `precision_macro`;
- `recall_macro`;
- `f1_macro`.

### Сравнение моделей

| Модель | Описание | Accuracy | Macro-F1 | Комментарий |
|---|---|---:|---:|---|
| DummyClassifier | Заглушка, предсказывает самый частый класс | 0.3361 | 0.1677 | Нижняя граница качества |
| MultinomialNB | TF-IDF + Naive Bayes | 0.7254 | 0.7268 | Быстрая классическая модель |
| LogisticRegression | TF-IDF + Logistic Regression | 0.7481 | 0.7492 | Лучшая baseline-модель |

Также была построена confusion matrix для лучшей модели:

```text
reports/confusion_matrix.png
```

Метрики экспериментов сохранены в:

```text
reports/metrics.json
```

### Выбор финальной модели

Финальной моделью выбрана `LogisticRegression` с TF-IDF-признаками.

Причины выбора:

- лучшая `macro-F1` среди протестированных моделей;
- высокая скорость обучения и инференса;
- простота интеграции в API;
- возможность получать вероятности классов через `predict_proba`;
- интерпретируемость и устойчивость классического ML-подхода.

Итоговая модель сохранена как sklearn Pipeline:

```text
TfidfVectorizer + LogisticRegression
```

Это удобно, потому что сервису не нужно отдельно выполнять векторизацию текста: весь пайплайн загружается из одного файла `sentiment_model.joblib`.

---

## 6. Архитектура решения и сервис

### Архитектура пайплайна

Общая схема проекта:

```text
Датасет отзывов
        ↓
EDA и очистка данных
        ↓
Обучение baseline-моделей
        ↓
Выбор финальной модели
        ↓
Сохранение sklearn Pipeline
        ↓
FastAPI-сервис
        ↓
Docker-контейнер
```

Основные файлы сервисной части:

| Файл | Назначение |
|---|---|
| `src/preprocessing.py` | очистка текста |
| `src/config.py` | пути и параметры проекта |
| `src/train.py` | обучение и сохранение модели |
| `src/predict.py` | загрузка модели и предсказание |
| `src/api.py` | FastAPI-приложение |
| `tests/test_api.py` | тесты API |
| `tests/test_predict.py` | тесты функции предсказания |

### API и endpoints

В проекте реализован FastAPI-сервис с двумя основными endpoint.

#### `GET /health`

Проверяет, что сервис запущен и модель доступна.

Пример ответа:

```json
{
  "status": "ok",
  "model_loaded": true
}
```

#### `POST /predict`

Принимает JSON с текстом отзыва.

Пример запроса:

```json
{
  "text": "Качество отличное, товар понравился"
}
```

Пример ответа:

```json
{
  "sentiment": "positive",
  "confidence": 0.971962339335859
}
```

Если пользователь передаёт пустой текст, сервис возвращает ошибку валидации.

### Технологический стек

В проекте используются:

- Python;
- pandas;
- scikit-learn;
- joblib;
- FastAPI;
- Pydantic;
- Uvicorn;
- pytest;
- Docker.

Локальный запуск сервиса:

```bash
uvicorn src.api:app --reload
```

Запуск через Docker:

```bash
docker build -t sentiment-review-service .
docker run --rm -p 8000:8000 sentiment-review-service
```

---

## 7. Наблюдаемость, конфигурация и безопасность

### Логи и наблюдаемость

В проекте реализовано базовое логирование через стандартный модуль `logging`.

Логируются:

- успешная загрузка модели;
- длина входного текста;
- предсказанный класс;
- confidence score;
- время обработки запроса;
- ошибки предсказания.

Пример логики находится в:

```text
src/api.py
```

Также реализован endpoint:

```text
GET /health
```

Он позволяет быстро проверить, что сервис работает и модель доступна.

### Конфигурация

Основные параметры вынесены в:

```text
src/config.py
configs/.env.example
```

В `.env.example` указаны:

```env
MODEL_PATH=artifacts/sentiment_model.joblib
LOG_LEVEL=INFO
MAX_TEXT_LENGTH=5000
```

Эти параметры можно изменить без переписывания основной логики сервиса.

### Безопасность

В проекте не используются реальные токены, пароли или приватные ключи.

Файл `.env` не коммитится в репозиторий.  
В репозиторий добавлен только пример конфигурации:

```text
configs/.env.example
```

Данные из `data/raw/` и `data/processed/` не хранятся в GitHub, так как они добавлены в `.gitignore`.

---


## 8. Ограничения и дальнейшая работа

### Ограничения

Текущая версия проекта имеет следующие ограничения:

- датасет относится к одной товарной категории: женская одежда и аксессуары;
- модель может хуже работать на отзывах из других доменов;
- короткие отзывы могут быть неоднозначными;
- используется классический ML-подход без трансформеров;
- нет полноценного пользовательского интерфейса;
- нет авторизации;
- не сохраняется история запросов пользователей.

### Дальнейшая работа

Возможные направления развития проекта:

- добавить определение темы отзыва: доставка, качество, цена, сервис, другое;
- добавить Streamlit-интерфейс или простое desktop-приложение;
- добавить загрузку CSV-файла с отзывами и агрегированную статистику;
- добавить endpoint `/metrics`;
- сравнить Logistic Regression с Linear SVM;
- добавить docker-compose для запуска одной командой;
- расширить датасет отзывами из других доменов;
- провести сравнение с предобученной transformer-моделью.

---

## 9. Сценарий демонстрации на защите

На защите будет показан основной сценарий работы проекта: запуск FastAPI-сервиса и получение предсказания тональности для пользовательского отзыва.

### Шаг 1. Запуск сервиса

Из папки `project` запускается FastAPI-сервис:

```bash
uvicorn src.api:app --reload
```

После запуска открывается Swagger UI:

```text
http://127.0.0.1:8000/docs
```

### Шаг 2. Проверка состояния сервиса

Сначала демонстрируется endpoint:

```text
GET /health
```

Он показывает, что сервис запущен и модель загружена.

Пример ответа:

```json
{
  "status": "ok",
  "model_loaded": true
}
```

### Шаг 3. Проверка предсказания

Затем демонстрируется endpoint:

```text
POST /predict
```

Позитивный пример:

```json
{
  "text": "Качество отличное, товар понравился"
}
```

Ответ:

```json
{
  "sentiment": "positive",
  "confidence": 0.971962339335859
}
```

Негативный пример:

```json
{
  "text": "Ужасное качество, товар пришел сломанный"
}
```

Ответ:

```json
{
  "sentiment": "negative",
  "confidence": 0.9758126291885473
}
```

### Шаг 4. Дополнительно

При необходимости можно показать запуск через Docker:

```bash
docker build -t sentiment-review-service .
docker run --rm -p 8000:8000 sentiment-review-service
```

Основной акцент при демонстрации: проект использует реальную обученную ML-модель, сервис возвращает тональность отзыва и confidence score, а также поддерживает запуск через FastAPI и Docker.

Ссылайтесь на команды и инструкции из `project/README.md`.

---
